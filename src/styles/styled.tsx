import { styled as nativeWindStyled } from "nativewind";
import { Text, View, TouchableOpacity, TextInput, Image, ScrollView, FlatList } from "react-native";

export const Text = nativeWindStyled(Text);
export const View = nativeWindStyled(View);
export const TouchableOpacity = nativeWindStyled(TouchableOpacity);
export const TextInput = nativeWindStyled(TextInput);
export const Image = nativeWindStyled(Image);
export const ScrollView = nativeWindStyled(ScrollView);
export const FlatList = nativeWindStyled(FlatList);